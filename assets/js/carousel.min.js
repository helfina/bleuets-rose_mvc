const blockOne = document.querySelector(".blockOne");
const blockTwo = document.querySelector(".blockTwo");
const Tab = [];
const block = [...blockOne];
console.log(Tab);
const index1 = 0;
// On récupère les deux flèches
let next = document.querySelector(".suivant");
let prev = document.querySelector(".precedent");

next.addEventListener("click", ChangeSlide());
prev.addEventListener("click", ChangeSlide());


// function ChangeSlide() {
//     index1 += index1 < 0 ? index1 = block.length - 1 : index1 > block.length - 1 && (index1 = 0), document.getElementsByClassName("carousel").element = block[index1]
// }

// console.log(block[1]);

// setInterval((function ChangeSlide() {
//     1 === index1 ? (blockOne.style.display = "none", blockTwo.style.display = "flex") : 0 === index1 ? (blockOne.style.display = "flex", blockTwo.style.display = "none") : (index1++, 1 == index1 && (index1 = 0))
// }))
// console.log(ChangeSlide());